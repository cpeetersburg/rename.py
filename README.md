This is a script to rename files and directories so that they are easily accessible from a Unix CLI.

## How to use it
1. Place the script in the parent directory of the files and directories that you want to rename.
2. Make the script executable: `chmod +x rename.py`
3. Execute the script: `./rename.py`
